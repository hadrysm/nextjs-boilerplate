import Head from 'next/head';
import Link from 'next/link';
import { ReactNode } from 'react';

import { NextPageWithLayout } from './_app';

const CustomLayoutPage: NextPageWithLayout = () => (
  <div>
    <Head>
      <title>Custom layout | Nextjs boilerplate</title>
      <meta name="description" content="Generated by create next app" />
      <link rel="icon" href="/favicon.ico" />
    </Head>
    <h1 className="text-center mt-8 font-bold text-2xl text-rose-800">
      Nextjs boilerplate - Custom layout
    </h1>
  </div>
);

CustomLayoutPage.getLayout = (page: ReactNode) => (
  <div>
    <header className="p-4 bg-rose-800">
      <ul className="flex items-center gap-10 text-gray-50">
        <li>
          <Link href="/">
            <a className="p-2 inline-block transition-colors hover:text-orange-100">Home</a>
          </Link>
        </li>
        <li>
          <Link href="/custom-layout" className="p-2">
            <a className="p-2 inline-block transition-colors hover:text-orange-100">
              Custom layout
            </a>
          </Link>
        </li>
      </ul>
    </header>
    <main className="bg-[#FCFCFC]">{page}</main>
  </div>
);

export default CustomLayoutPage;
